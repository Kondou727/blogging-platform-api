version: "2"
sql:
    - schema: "sql/schema"
      queries: "sql/queries"
      engine: "sqlite"
      gen:
          go:
              package: "database"
              out: "/internal/database/"
              overrides:
                  - column: "blogs.tags"
                    go_type: "encoding/json.RawMessage"
